# Demo 迭代计划

## 当前情况（2026-09-05）

上一版 5b574ab 有枪口焰、抛壳和开场等待，但不能据此认定四项改造全部完成。
三把枪共用静态手臂；换弹和泵动移动的是整体。步枪后坐每发增加 0.035，恢复速度为 1.15/秒，10 发/秒时会在下一发前归零。暂停不冻结计时，兼容模型没有新动画所需的基础参数。弹壳和火花移出场景后没有释放资源。

## 第一轮：战斗状态可靠性

- [x] 暂停冻结通关计时、准备时间、换弹、开火冷却、敌人攻击和特效计时；清理按键，恢复后不自动走路或射击。
- [x] 步枪连射累计后坐，停火恢复；手枪和霰弹枪保留独立恢复速度。
- [x] 为兼容模型补齐动画参数，切枪清除上把枪的后坐。
- [x] 释放过期特效的几何与材质，去掉无效材质参数。
- [x] 使用真实函数进行自动回归测试，覆盖暂停、连射、切枪和资源释放。
- [ ] 完成浏览器实射和全流程通关验收。用户要求先发布当前迭代版供实际体验，未验收项继续保留。

## 第二轮：关卡通行

第一轮自动测试 10/10 通过，浏览器进入加载页后测试标签意外关闭，尚未完成画面验收。
现有场地右侧空旷，可以绕开主路线全部掩体；部分无人机出生点与侧路长掩体重叠。

- [x] 收窄左右边界，用分段隔墙分出主路和侧路，保留入口、中段、末段连接口。
- [x] 调整全部出生点，保证碰撞半径内无掩体；每波准备时间至少 2 秒。
- [x] 敌人遇墙时绕行；固定案例验证绕过掩体到达目标，不等同于所有动态情况均已验收。
- [x] 用实际碰撞函数验证出生点、两路连通、撤离可达和敌人绕行。

画面复测发现中空美术模型与实心碰撞不一致，已补回实心结构并截图确认。另补了 Esc 主动暂停，不再完全依赖鼠标解锁事件。

### 本轮测试结果

运行 `node --test "04 vertex-demo/tests/combat.test.cjs" "04 vertex-demo/tests/navigation.test.cjs"`：17 项全部通过。测试直接加载页面中的实际函数，Three.js 渲染、音频和射线命中使用替身，不代表真实枪感通过验收。

浏览器已验证资源加载、开始游戏、步枪/霰弹枪切换、实心掩体、Esc 暂停和恢复后保留准备时间。内嵌浏览器点击没有触发实射（弹药仍为 6/6），鼠标锁定实射、连射画面和整局通关尚未验证。本轮按用户要求提交当前迭代版到 GitHub Pages，作为待继续验收的体验版本，不代表所有改造均已完成。

## 后续：手臂和动作（独立验收）

- [ ] 分别适配右手握把、左手护木位置；手枪双手握持。
- [ ] 独立移动弹匣、支撑手和泵柄，检查动画中手掌接触关系。
- [ ] 检查主路和侧路全程可通行、出生点不在碰撞体内、敌人能绕过掩体。
- [ ] 实际走完三波到撤离；截图或录像验证三把枪。

每轮记录：发现的问题 → 修改 → 测试结果 → 尚未通过的项。部署成功只证明网站发布成功，不代表动作或关卡验收通过。
